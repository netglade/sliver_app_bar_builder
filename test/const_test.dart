import 'package:flutter_test/flutter_test.dart';
import 'package:sliver_app_bar_builder/sliver_app_bar_builder.dart';

void main() {
  // ignore: missing-test-assertion, not needed for this test
  test('SliverAppBarBuilder can be const-constructed', () {
    // ignore: avoid-unused-instances, we test the construction
    const SliverAppBarBuilder(barHeight: 42);
  });
}
