name: sliver_app_bar_builder_example
description: SliverAppBarBuilder storybook demo.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  sliver_app_bar_builder:
    path: ../
  storybook_flutter: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  netglade_analysis: ^8.0.0

flutter:
  uses-material-design: true
